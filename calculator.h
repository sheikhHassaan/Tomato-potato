#include<calculator.cpp>
class calculator{
    private:
    int sum;
    int diff;
    int prod;
    int div;
    
    private:
    int add(int num1, int num2);
    int multiply(int num1, int num2);
    int divide(int numi, int denumi);
    int subtract(int num1, int num2);
}